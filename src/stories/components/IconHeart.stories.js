import IconHeart from '../../components/IconHeart'

export default {
  title: 'Components/IconHeart',
  component: IconHeart,
  parameters: {
    layout: 'centered'
  }
}

const Template = () => <IconHeart />

export const Default = Template.bind({})