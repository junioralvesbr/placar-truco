import IconSpade from '../../components/IconSpade'

export default {
  title: 'Components/IconSpade',
  component: IconSpade,
  parameters: {
    layout: 'centered'
  }
}

const Template = () => <IconSpade />

export const Default = Template.bind({})